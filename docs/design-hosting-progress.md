# Hosting progress — taking jobsearch online (multi-user)

Living status doc for the work of turning the local app into a hosted,
sign-up-and-use product reachable from any device (Android / iOS / web — one
responsive website covers all three). Companion to:

- [`design-hosting.md`](design-hosting.md) — *what changes* between the local
  app and a hosted product (the data model, which features can't be hosted).
- [`design-deployment.md`](design-deployment.md) — *where to run it and what it
  costs*.

This doc tracks *what's actually done* and *what's next*.

## Why "connect Supabase" isn't enough on its own

Supabase hosts your **data** (managed Postgres + Auth + storage). It does not
run your **app** — the FastAPI server still needs a compute host, and the code
needs to (a) speak Postgres, (b) know who the logged-in user is, and (c) scope
every row to that user. Supabase covers the database and auth slots of that
plan; the app host and the code changes are the rest.

## Status at a glance

| Stage | Scope | State |
|---|---|---|
| **1. Postgres backend + schema** | DB layer runs on Postgres; schema live on Supabase | ✅ **Done, tested** |
| **1b. Dialect cleanup (deferred modules)** | Date math in the email + report modules | ⬜ Not started |
| **2. Auth + multi-tenancy** | `users`, login, `user_id` scoping, admin | ⬜ Not started (the bulk) |
| **3. Deploy** | Containerize FastAPI, run on a public host with HTTPS | ⬜ Not started |
| **4. Repoint the daily worker** | GitHub Action writes Postgres instead of committing files | ⬜ Not started |
| **5. Hardening** | The security checklist in `design-hosting.md` | ⬜ Ongoing |

## Stage 1 — Postgres backend + schema (DONE)

The app was single-user SQLite (`webapp/db.py`). It now runs on **either**
SQLite (local default, unchanged) **or** Postgres, chosen at runtime.

**What shipped**

- `webapp/pgcompat.py` — a psycopg (v3) adapter exposing the small slice of the
  `sqlite3.Connection` API the codebase uses (`execute` / `executescript` /
  `commit` / `close`, dict rows), translating SQLite placeholders (`?`,
  `:name`) to psycopg's (`%s`, `%(name)s`), and translating the schema DDL.
- `webapp/db.py::connect()` selects the backend: **SQLite by default**;
  **Postgres when `JOBSEARCH_DATABASE_URL` is set**. Local dev and the existing
  test suite are byte-for-byte unaffected.
- The handful of SQLite-only constructs were made dialect-portable (verified to
  still behave identically on SQLite):
  - `cursor.lastrowid` → `INSERT … RETURNING id` (psycopg has no lastrowid).
  - `INSERT OR IGNORE` → `INSERT … ON CONFLICT DO NOTHING`.
  - `ORDER BY … COLLATE NOCASE` → `ORDER BY LOWER(…)`.
  - `current_role` column quoted as `"current_role"` (it's a reserved word in
    Postgres; double-quoting is valid in both engines).
  - `INTEGER PRIMARY KEY` → `… GENERATED BY DEFAULT AS IDENTITY`; `REAL` →
    `DOUBLE PRECISION` (schema transform).
- `supabase/migrations/0001_initial_schema.sql` — the generated Postgres
  schema, **applied to the Supabase project** (all 24 tables live).
- `psycopg[binary]` added to `requirements.txt` / `Pipfile` (only imported when
  running on Postgres).

**How it's verified**

- The full existing suite stays green on SQLite (no regression).
- `tests/test_pgcompat.py` — unit tests for the translation/transform helpers.
- `tests/test_postgres_backend.py` — the **real `db.py` code paths run against
  live Postgres** (jobs lifecycle, search, `RETURNING` id, `ON CONFLICT`,
  `LOWER` ordering, the full prep-content seed). Gated on
  `JOBSEARCH_TEST_DATABASE_URL`, so CI without Postgres still passes.

```bash
# Run the Postgres parity suite against any throwaway Postgres:
JOBSEARCH_TEST_DATABASE_URL=postgresql://user:pass@host/db \
  python -m pytest -q tests/test_postgres_backend.py
```

**What Stage 1 deliberately does _not_ do:** it does not add authentication or
make the data multi-user. Pointing the app at Postgres today gives every
visitor the *same shared* data — fine for the parity work, not safe to expose.
That's Stage 2.

## Stage 1b — dialect cleanup for the deferred modules

Two modules still use SQLite-only date functions (`julianday`, `strftime`):
`webapp/gmail.py` and `jobsearch/report.py`. Neither is on the hosted-v1
critical path — Gmail sync is deferred (see `design-hosting.md`), and the
Markdown report generator runs in the CLI/worker, not the web request path — so
they were left for a focused follow-up. Port them (compute the interval in
Python, or use `AGE()`/`EXTRACT` on Postgres) before enabling those features
on the hosted backend.

## Stage 2 — auth + multi-tenancy (the bulk)

Per `design-hosting.md`: add a `users` table; add a `user_id` column + index to
the per-user tables (`applications`, `application_events`, `profile_fields`,
`runs`, `prep_*_progress`, `company_problem_progress`) while `jobs`/`job_events`,
the prep **content** tables, and `company_problems` stay global; move per-user
fit into a `user_job_fit` table. Signup / login / logout; every query scoped by
the session's user id via one `current_user` dependency. Admin page behind
`is_admin`.

**Two decisions to make before writing Stage 2:**

1. **Auth provider — Supabase Auth vs app-native sessions.** Recommended:
   **Supabase Auth** (hosted login, email verification, password reset, "Sign
   in with Google"). It removes the riskiest security surface — you never store
   passwords — and you're already on Supabase. App-native (argon2id + signed
   cookies) is more code and more ways to get it wrong.
2. **Row-Level Security (RLS).** Supabase enabled RLS on all 24 tables by
   default (they're currently locked to the service role). If the FastAPI app
   connects with a **direct Postgres role** and enforces `user_id` in its own
   queries (the plan in `design-hosting.md`), RLS is a defense-in-depth backstop
   and we add policies later. If instead the browser talks to Supabase
   **directly** (PostgREST), RLS policies become the *primary* authorization
   and must be written first. The current architecture is the former.

## Stage 3 — deploy the app

Containerize the FastAPI app (lean image: no Chromium, sklearn/numpy off the
request path) and run it on Fly.io / Railway / Cloud Run with platform-
terminated HTTPS. Config via env vars: `JOBSEARCH_DATABASE_URL` (the Supabase
connection string — use the **pooler** endpoint for serverless/multi-worker),
a session signing secret, etc. See `design-deployment.md` for host comparison
and cost (~$0–5/mo to start).

## Stage 4 — repoint the daily worker

The existing `.github/workflows/daily-job-search.yml` already scrapes boards
daily. Change only its final step: instead of committing `reports/`, write
postings to Postgres (`JOBSEARCH_DATABASE_URL` secret) and trigger per-user
rescoring. Add a keepalive so GitHub doesn't disable the schedule after 60 days
of (commit) inactivity.

## Stage 5 — hardening

Work the security checklist in `design-hosting.md` §"Security checklist":
session cookie flags, CSRF on state-changing forms, upload caps + MIME checks,
rate limits on auth/upload, HTTPS/HSTS/CSP headers, automated Postgres backups,
account-deletion endpoint, and encrypt resumes at rest.
